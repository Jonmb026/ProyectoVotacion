@model ProyectoVotacion.Models.LoginViewModel

@{
    Layout = "_LoginLayout";
    ViewData["Title"] = "Iniciar Sesión";
}

<div class="login-container">
    <div class="login-left">
        <div class="logo">
            <img src="~/images/logo.png" alt="Logo">
        </div>
        <h2>Bienvenido al Sistema de Votación</h2>
        <form asp-action="Login" method="post">
            <div class="form-group">
                <label asp-for="Email">Correo Electrónico</label>
                <input asp-for="Email" class="form-control" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Password">Contraseña</label>
                <input asp-for="Password" class="form-control" type="password" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Iniciar Sesión" class="btn-login" />
            </div>
        </form>
        <div class="form-text">
            <a href="#">¿Olvidaste tu contraseña?</a>
        </div>
        <div class="form-text mt-3">
            <span>¿No tienes una cuenta? <a href="@Url.Action("Register", "Auth")" class="btn-signup">Crear una nueva</a></span>
        </div>
    </div>
    <div class="login-right">
        <h2>El poder de tu voto</h2>
        <p>Votar es un derecho y una responsabilidad fundamental en cualquier democracia. Al votar, no solo eliges a tus líderes, sino que también participas activamente en la construcción del futuro de tu país. Cada voto cuenta y tiene el poder de generar cambios significativos. A través de este sistema, te brindamos la oportunidad de hacer oír tu voz y contribuir al bienestar de toda la nación.</p>
    </div>
</div>

@section Scripts {
    <!-- Aquí puedes agregar scripts adicionales si es necesario -->
}
